from django.urls import path
from . import views
from rest_framework import routers 

# # urlpatterns = [
# #     path('', views.Category.as_view()) ,
# # ]

# router = routers.SimpleRouter()

# router.register(r"traffic-potential", TrafficPotentialViewSet)
# # router.register(r"competitor-comparison", CompetitorComparisonViewSet)
# # router.register(r"management-overview", ManagementOverViewViewSet)
# # router.register(r"database", DatabaseViewSet)
# # router.register(r"landing-page", LandingPageViewSet)
# # router.register(r"", ToplistViewSet)

# urlpatterns = []
# urlpatterns += router.urls
